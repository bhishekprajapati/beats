import { Component } from '@angular/core';
import { MaxContainer } from 'src/app/layouts/max-container/max-container';

@Component({
  selector: 'app-header',
  imports: [MaxContainer],
  templateUrl: './header.html',
})
export class Header {}
